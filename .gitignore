# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
dist/
build/

# TypeScript incremental
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local env files
.env*.local
.env

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
# Local persistence (SQLite). Folder itself stays tracked via .gitkeep
# so a fresh clone preserves the path; only the DB files are excluded.
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Editor / tool local state
.puku/
